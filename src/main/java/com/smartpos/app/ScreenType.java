package com.smartpos.app;

public enum ScreenType {
    LOGIN,
    MAIN_POS,
    PRODUCTS,
    REPORTS,
    SYNC,
    SETTINGS,
    USER_MANAGEMENT,
    REGISTRATION,
    CREDIT_MANAGEMENT
}
